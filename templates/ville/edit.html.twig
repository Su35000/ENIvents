{% extends 'base.html.twig' %}

{% block title %}Hello VilleController!{% endblock %}

{% block body %}
<h1> Gérer les villes </h1>
    <br><br><br>

    Filtrer les villes
    Le nom contient :
    <input type="search">
    <button>Rechercher</button>
    <div class="ville-panel">
        <div class="nom-ville-pannel">
            <ul>
            {% for ville in villes %}
                <li>{{ ville.nom() }}</li>
            {% endfor %}

            </ul>
        </div>
        <div class="codepostal-ville-pannel">
            <ul>
                {% for ville in villes %}
                    <li>{{ ville.codePostal }}</li>
                {% endfor %}
            </ul>
        </div>
        <div class="actions-ville-pannel">
            <ul>
                {% for ville in villes %}
                    <li><a href="">Modifier</a> - <a href="{{ path('ville_delete', {'id':ville.id()}) }}">Supprimer</a> </li>
                {% endfor %}
            </ul>
        </div>

    </div>
{% endblock %}
